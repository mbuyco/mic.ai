#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ "$#" -eq 0 ]]; then
  exec "$ROOT_DIR/bin/compose" logs -f api worker scheduler postgres redis
fi

exec "$ROOT_DIR/bin/compose" logs -f "$@"
